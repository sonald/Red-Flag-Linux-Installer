script(type="text/template", id="primary_part_tmpl").
    li.primary.horizontal(style="width:"+part.percent+"%;")
        button.delete.close(type="button",number=part.number,path=path)×
        ul.selectable.wat-cf.part(number=part.number,pty="primary", data-toggle="modal", href="#"+String(dindex)+String(pindex),dindex=dindex,pindex=pindex)
            li
                span= part.ui_path
                span.partmp
            li
                span.partfs= part.fs
                span= " " + part.size + part.unit
