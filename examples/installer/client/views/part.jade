script(type="text/template", id="part_tmpl").
    fieldset
        div.control-group
            label.control-label(for="name") name
            .controls
                input.required#name(type="text", minlength="2", size="25", name="name")
        div.control-group
            label.control-label(for="password") password
            .controls
                input.required#password(type="password", size="25", name="password")
        div.control-group
            label.control-label(for="confirm_password") confirm_password
            .controls
                input.required#confirm_password(type="password", size="25", name="confirm_password")
        div.control-group
            div#getpartitions.controls
                div#accordion2.accordion
                    - for (var i in disks)
                        - var disk = disks[i];
                        div.accordion-group
                            div.accordion-heading
                                a.accordion-toggle(data-toggle="collapse", data-parent="#accordion2", href="#collapse"+i)
                                    = disk["path"]
                            div.accordion-body.collapse.in(id="collapse"+i)
                                div.accordion-inner.controls
                                    table.table.table-bordered
                                        thead
                                            tr
                                                th 
                                                th Number
                                                th Start(MB)
                                                th End(MB)
                                                th Size(MB)
                                                th parttype
                                                th fs
                                        tbody
                                            - var parts = disk["table"]
                                            - for ( var part in parts )
                                                - var p = parts[part]
                                                - if(p[0] == -1)
                                                    - continue
                                                tr
                                                    th
                                                        label.radio
                                                            input(type='radio', name="parts", value=disk["path"]+p[0])
                                                    - for (var x in p )
                                                        th
                                                            =p[x]

