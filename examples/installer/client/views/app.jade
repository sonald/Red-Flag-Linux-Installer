extends layout

block header
    //-script(src="/app.js")
    title= tr('Hippo')

block content
    h1: center= tr('Welcome To ') + tr(name)+ tr(' Installer') 
    
    div.breadcrumb-wrapper
        .myicon-spacer
            ol#breadcrumb-trail
                li.mybreadcrumb.category.last
                    .myicon-container
                        .breadcrumb-myicon
        .end-trail
            span= tr("Let's ") + tr(name)

    .body-expand
        #stage

    .footer
        ul.buttons

    include license
    include license_zh
    include restore
    include part
    include process
    include finished
    include partial
